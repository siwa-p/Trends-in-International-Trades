[pytest]
testpaths = tests 
python_files = test_*.py 
pythonpath = src 

addopts = -v --tb=short
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
log_cli_level = INFO
filterwarnings =
    ignore::DeprecationWarning